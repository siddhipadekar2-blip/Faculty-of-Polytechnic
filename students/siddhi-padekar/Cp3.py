#problem1
def collect_all_sub():
    sub = []

    ele = collect_one_sub()
    subs. append(ele)

    while len(susb) <= MAX_SUB:
        if ask_yes_no("Add another sub "):
          ele  = collect_one_sub()
          subs. applend
        else:
            break
